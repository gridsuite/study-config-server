server:
  port: 5035

powsybl-ws:
  database:
    host: localhost

spring:
  rabbitmq:
    addresses: localhost

gridsuite:
  services:
    single-line-diagram-server:
      base-uri: http://localhost:5005